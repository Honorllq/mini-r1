# Mini-R1 依赖清单
# 安装：pip install -r requirements.txt

# 核心训练
torch>=2.4.0
# TRL 0.24-0.25 与 Transformers 4.x 的 GRPO API 已验证兼容
transformers>=4.56.1,<5.0.0
trl>=0.24.0,<0.26.0
peft>=0.13.0
accelerate>=1.0.0
datasets>=3.0.0

# 加速
unsloth>=2024.10
bitsandbytes>=0.44.0
flash-attn>=2.6.0; platform_system != "Windows"

# 推理 + 评测
vllm>=0.6.0; platform_system != "Windows"

# 实验跟踪
wandb>=0.18.0
tensorboard>=2.18.0

# 工具
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.9.0
tqdm>=4.66.0
